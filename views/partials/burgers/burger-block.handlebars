<li>
	{{burger_name}}

	<button class="btn btn-info btn-sm" id="changeDevoured" data-id="{{id}}" data-devouredState="{{devoured}}">
		{{#if devoured}}Eat Me!{{else}}Make It Again!{{/if}}
	</button>
</li>
<br>